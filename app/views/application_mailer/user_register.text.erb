Welcome
===============================================

<%= "#{@user.firstname} #{@user.lastname} #{@user.secondname}" %>,

для подтверждения регистрации пройдите по ссылке <%= confirmation_url(token: @user.token) %>