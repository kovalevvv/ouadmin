Задать пароль
===============================================

<%= "#{@user.firstname} #{@user.lastname} #{@user.secondname}" %>,

чтобы задать пароль пройдите по ссылке <%= set_password_url(host: Setting.self_host, port: Setting.self_port, token: @user.token) %>