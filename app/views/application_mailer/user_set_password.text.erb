Задать пароль
===============================================

<%= "#{@user.firstname} #{@user.lastname} #{@user.secondname}" %>,

чтобы задать пароль пройдите по ссылке <%= set_password_url(token: @user.token) %>